# Description
Its copy of yes command.

# Run (c)
```bash
gcc main.c -o yes
./yes [some_text] # standartly its writes y
```

# Run (c++)
```bash
g++ main.c -o yes
./yes [some_text] # standartly its writes y
```
